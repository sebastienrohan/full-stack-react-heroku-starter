import React from 'react'
import { Card, CardTitle } from 'material-ui/Card'

const NotfoundPage = () => (
  <Card className='container'>
    <CardTitle title='Page not found' subtitle='This is not the page you are looking for.' />
  </Card>
)

export default NotfoundPage
